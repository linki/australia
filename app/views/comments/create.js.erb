<% if @comment.valid? %>
  $(".comments").append("<%= escape_javascript render(:partial => @comment) %>");
  $("#comment_text").val('');
  $.gritter.add({
    title: 'Notice',
    text: '<%= flash[:notice] %>'
  });
  <% flash.discard %>
<% else %>
  alert('enter a text');
<% end %>
